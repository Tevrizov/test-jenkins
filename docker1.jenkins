pipeline {
  agent { 
    dockerfile {true
    label 'docker1'
    }
   }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}