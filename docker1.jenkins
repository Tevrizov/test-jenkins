pipeline {
  agent { 
       dockerfile true 
    }
  stages {
    stage('Test') {
      agent {
        label 'docker1' 
      }
      steps {
        sh '''
          node --version
          curl --version
        '''
      }
    }
  }
}
