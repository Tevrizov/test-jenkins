pipeline {
  agent { 
       label 'docker1'
       dockerfile true 
    }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          curl --version
        '''
      }
    }
  }
}
